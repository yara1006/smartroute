# SmartRoute TODO

## P0 - 评分硬指标

- [x] 将 Web Demo 改为“左侧场景选择器 + 中间美团手机模拟器 + 右侧 Agent 解释面板”。
- [x] 更新移动端为“原生美团页面 → SmartRoute 插件入口 → 路线页”的动线，而不是独立 App 首页。
- [x] 增加四个入口场景：搜索页、问小团、收藏夹、POI 详情页。
- [x] 问小团入口增加路线意图识别：首屏保持通用小团，不默认显示路线规划 prompt；高置信调起 SmartRoute，中置信先确认，低置信保持普通问答。
- [x] 路线规划稳定支持 >=3 个 POI。
- [x] 主路线至少覆盖餐饮 + 娱乐/文化两类。
- [x] 在路线解释中显示时间、距离、预算、排队约束命中情况。
- [x] 增加未知 POI 场景的演示入口，例如“我下午要去外滩玩 3 个小时”。
- [x] 更新 README 和 docs，统一产品定位。

## P1 - 体验创新

- [x] 新增 `/api/adjust`，支持基于当前路线的自然语言实时调整。
- [x] 前端增加底部“继续调整”输入栏。
- [x] 增加调整历史，展示每次调整影响。
- [x] 增加模拟美团 App 用户画像上下文：搜索偏好、收藏偏好、浏览/到店偏好、常用预算、常去商圈、排队容忍度，并在路线召回和排序中加权。
- [x] 小团高置信调起 SmartRoute 时，将“当前自然语言输入 + 模拟美团用户画像”同时传入路线生成链路，避免只按一句 prompt 规划。
- [x] 生成路线后增加满意度追问/确认卡：当路线置信度低、约束缺失或画像冲突时，询问用户是否更想要“少排队 / 更便宜 / 更文艺 / 少走路”，并基于回答局部调整路线。
- [x] 增加三种用户画像模式：
  - 低排队务实型
  - 文艺体验型
  - 带爸妈轻松型
- [x] 同一个 prompt 切换不同画像后，生成差异化路线。
- [x] 增加冲突解释：预算冲突、排队冲突、时间冲突、偏好冲突。
- [x] 增加 `planning_time_ms`，展示规划耗时。
- [x] 增加 `profile_influence`，展示画像信号如何影响 POI 召回和路线排序。
- [x] 将追问卡结构化为 `follow_up.options`，点击选项可直接触发 `/api/adjust`。
- [x] `/api/adjust` 返回 `adjustment_status`，区分已应用、部分应用和未应用。
- [x] `/api/adjust` 返回调整前后指标、指标差值和变化站点。
- [x] 调整失败时返回 `suggested_relaxations`，提示放宽预算、排队、区域或时间。
- [x] 前端展示调整结果卡、指标 delta、站点变化和失败补救建议。
- [x] Agent 解释面板增加“画像信号 → 召回加权 → 路线变化”和固定演示脚本。
- [x] P1c 增加画像来源机制：`preset`、`manual_import`、预留 `official_api`。
- [x] P1c 增加 `/api/profile-sources`，列出模拟画像、脱敏导入画像和官方 API 状态。
- [x] P1c 增加 `/api/profile/import`，支持导入搜索、收藏、浏览、预算、排队、步行等脱敏画像。
- [x] P1c 在 `/api/plan` 支持 `profile_source` 和 `profile_id`，脱敏画像优先参与路线召回和排序。
- [x] P1c 前端支持“模拟画像 / 脱敏真实画像”切换、选择导入样本和粘贴 JSON 导入。
- [x] P1c 明确拒绝账号、手机号、cookie、token、订单号等敏感字段。
- [x] P1d 去掉默认外滩自动规划，按用户输入或入口上下文动态确定城市和地图锚点。
- [x] P1d `/api/plan` 支持 `route_context`：小团/搜索锚点、收藏夹已选 POI、详情页当前商户坐标。
- [x] P1d 接入高德 Web 服务 adapter，支持地点解析、周边 POI 召回和路径 polyline；P2 已收紧为明确地点场景不跨城回退本地 RAG。
- [x] P1d 收藏夹已选 POI 作为 pinned stops，路线补齐而不是忽略用户选择。
- [x] 交付日修复：POI 详情页支持 `fixed_start_poi_id`，从当前商户出发时第 1 站固定，调整后仍保留。
- [x] 交付日修复：搜索页新增 `/api/search-preview`，先展示真实候选 POI，再把勾选 POI 作为 soft pinned stops 进入路线规划。
- [x] 交付日修复：高德 Web 服务请求增加 30 分钟内存 TTL 缓存，降低公网演示重复调用导致 QPS/额度风险。

## P2 - 技术增强

- [x] 增加 DeepSeek Key 配置说明，Key 只放后端 `.env`，无 Key 时规则兜底。
- [x] 将 Intent Parser 升级为 LLM + 规则兜底，返回 `parser_source`、`parser_confidence`、`parser_reason` 和 `llm_slots`。
- [x] 新增评委即时画像弹窗，首次调起 SmartRoute 时采集同行人群、预算、排队、移动方式和内容偏好。
- [x] 将评委选择保存为 `judge-session` 脱敏画像，复用 `manual_import` 链路参与路线召回和排序。
- [x] 将实时调整做成 ReAct / ToolUse 风格工具调用，并返回 `tool_trace`：
  - Parse adjustment
  - Search replacement POI
  - Validate constraints
  - Update route
  - Explain changes
- [x] 深化高德路径规划：支持步行、公交/地铁、打车/驾车策略，返回更细的分段交通说明和降级原因。
- [x] 优化高德锚点解析：POI 文本搜索优先、地理编码兜底，避免“中山大学”等口语地点被错误解析成同名城市地址。
- [x] 明确地点场景强制使用高德真实 POI；高德失败时返回失败 trace，禁止跨城回退到上海本地 RAG。
- [x] 高德 adapter 记录最近错误，便于排查 Key 类型、服务权限、额度和网络问题。
- [x] Agent 解释面板展示 LLM 解析、即时画像、ToolUse trace、高德数据来源和调整前后变化。
- [x] 文档补充真实账号授权、数据最小化和合规边界说明。
- [ ] 接入真实美团 POI 或美团 LBS 数据源，替代当前高德/本地兜底 POI。
- [ ] 拿到美团或大赛方授权后，启用 `official_api` profile adapter。
- [ ] 增加端到端测试和前端自动化测试。

## P3 - 部署与交付

- [x] 上传 GitHub，确保不包含 `web/.env.local`、`.env`、高德 Key 或 DeepSeek Key。
- [x] README 增加本地启动、Key 配置和测试说明。
- [x] 准备固定公网 Demo：`http://42.193.138.163/`。
- [x] 明确 Stitch 不作为最终部署平台，只用于移动端视觉参考或导出高保真设计。
- 录制 1-2 分钟演示视频。
- 准备答辩脚本和 3 个固定演示场景。

## 当前演示场景

- 未知 POI：`我下午要去深圳大学附近玩3个小时，帮我规划一个路线`
- 城市切换：`我下午要去外滩玩3个小时，帮我规划一个路线`
- 收藏夹：勾选深圳大学/科技园附近 2-5 个收藏商户后点击“一键排路线”
- POI 详情页：从 `gaga（金地威新中心店）` 出发，补齐顺路可逛地点
- 搜索页：搜索 `广州永庆坊附近逛吃3小时`，先出现真实候选 POI，勾选后再智能排路线
- P2 即时画像：首次调起 SmartRoute 时选择“带爸妈 / 尽量不排队 / 少走路 / 展览文化”，观察路线和解释变化
- 真实地点：`在北京金鱼胡同附近玩3个小时`，应返回北京东城区高德 POI；如果高德失败，应返回失败 trace，不能返回上海 POI
- 低排队：`想吃上海特色，但不想排队超过15分钟，4个人，晚上6点出发`
- 带爸妈：`带爸妈在上海玩半天，少走路，轻松一点，预算人均300`
- 文艺体验：`帮我规划一个上海外滩附近的文艺下午，时间3小时，两个人，预算200，不想排队`

## 验收提醒

- 不要把未完成能力写成已完成。
- 不要让移动端继续像独立 App。
- 不要只展示 SmartRoute 独立页面，要展示它如何在美团场景中被调起。
- 不要只展示 POI 推荐，要展示路线执行。
- 不要重新规划替代实时调整。
- 不要提交任何 Key。
- 明确城市/地点输入时，不要为了“有结果”而回退到跨城本地模拟 POI。
